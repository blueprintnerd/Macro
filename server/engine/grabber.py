import requests
import os
import subprocess

def grab_link_or_file(path):
    try:
        resposes.get(path)
    except:
        try:
            # Find out soem way to get the contents of the file
        except:
            
    